from django.apps import AppConfig


class WindConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'WIND'  # Assurez-vous que ce chemin correspond à votre structure
